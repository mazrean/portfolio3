---
interface Props {
  maxWidth: string
}

const { maxWidth } = Astro.props
---

<p><slot /></p>

<style define:vars={{ maxWidth }}>
  p {
    margin: 0;
    text-align: center;
    max-width: var(--maxWidth);
    word-break: keep-all;
    overflow-wrap: break-word;
  }
</style>
