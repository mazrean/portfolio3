---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<h3>{title}</h3>

<style>
  h3 {
    font-size: 2rem;
    margin: 0;
    color: var(--accent);
    width: fit-content;
  }
</style>
